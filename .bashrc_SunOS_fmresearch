# Load information that only belongs with fminternal here

LD_LIBRARY_PATH="/data/software/builds/current/compiler-4.4.2/lib"
LD_LIBRARY_PATH=$LD_LIBRARY_PATH":/data/software/builds/current/server-3.0.1/lib"
LD_LIBRARY_PATH=$LD_LIBRARY_PATH":/data/utils/usr/local/lib"
LD_LIBRARY_PATH=$LD_LIBRARY_PATH":/data/utils/lib:/usr/ccs/lib:/usr/lib"
export LD_LIBRARY_PATH

#Make sure we can see maken pages for everything installed in the UTILS folder
MANPATH="/data/software/builds/current/server-3.0.0/man"
MANPATH="$MANPATH:/data/utils/share/man"
MANPATH="$MANPATH:/home/devel/GNU/man"
MANPATH="$MANPATH:/data/utils/man"
MANPATH="$MANPATH:/data/utils/usr/local/share/man"
MANPATH="$MANPATH:/usr/man:/usr/share/man"
export MANPATH

# Add some useful directories to our PATH if necessary. Add all the paths
# you want to the $PATH variable
PATH="/data/software/builds/current/compiler-4.4.2/bin"
PATH="$PATH:/data/software/builds/current/server-3.0.0/bin"
PATH="$PATH:/data/utils/bin"
PATH="$PATH:/data/utils/usr/local/bin"
PATH="$PATH:/usr/ccs/bin"
PATH="$PATH:/usr/bin"
#PATH="$PATH:/data/utils/bin"
#PATH="$PATH:/sbin"
#PATH="$PATH:/bin"
#PATH="$PATH:/usr/local/sbin"
#PATH="$PATH:/usr/local/bin"
#PATH="$PATH:/usr/pkg/bin"
#PATH="$PATH:/opt/sfw/bin"
#PATH="$PATH:/usr/sbin"
#PATH="$PATH:/usr/bin"
#PATH="$PATH:$ORACLE_HOME/bin"
#PATH="$PATH:/usr/ccs/bin"
#PATH="$PATH:/home/devel/GNU/bin"
export PATH

